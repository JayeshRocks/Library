specVersion: "v1beta"
serviceId: "Library"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Library-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
